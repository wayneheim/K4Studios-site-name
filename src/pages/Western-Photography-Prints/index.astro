---
export const prerender = true;

import KeywordLandingPage from '@/components/doorway/KeywordLandingPage.astro';

import {
  pagePath,
  imageSectionPath,
  gridBasePath,
  landing,
  breadcrumbItems,
  storyBlocks,
  explorationPaths,
  featuredReadingTitle,
  featuredReadingIntro,
  featuredReadingItems,
  collectionGroups,
  gridImages,
  collection,
  faqSection,
  faqItems,
  pageMeta,
  structuredAbout,
  webPageAbout,
  genre,
  collectionAltPrefix,
  hybridCarouselProps,
} from '@/data/hybrid-hubs/western-prints.ts';

import { galleryData as colorGallery }
  from '@/data/Galleries/Painterly-Fine-Art-Photography/Facing-History/Western-Cowboy-Portraits/Color.mjs';
---

<KeywordLandingPage
  pagePath={pagePath}
  imageSectionPath={imageSectionPath}
  gridBasePath={gridBasePath}
  landing={landing}
  breadcrumbItems={breadcrumbItems}
  storyBlocks={storyBlocks}
  faqItems={faqItems}
  explorationIntro="Western Art Discovery"
  explorationPaths={explorationPaths}
  featuredReadingTitle={featuredReadingTitle}
  featuredReadingIntro={featuredReadingIntro}
  featuredReadingItems={featuredReadingItems}
  collectionGroups={collectionGroups}
  gridImages={gridImages}
  collection={collection}
  faqSection={faqSection}
  galleryData={colorGallery}
  slides={[]}
  meta={{
    title: pageMeta.title,
    description: pageMeta.description,
    ogDescription: pageMeta.description,
  }}
  structuredAbout={structuredAbout}
  webPageAbout={webPageAbout}
  genre={genre}
  collectionAltPrefix={collectionAltPrefix}
  heroVisualMode="hybridCarousel"
  hybridCarouselProps={hybridCarouselProps}
  preHeroIntro="Western photography prints and Western photo prints by Wayne Heim are curated from cowboy portraits, Wild West narratives, and historically grounded Western images built for collector-grade wall presence rather than generic decor."
/>
