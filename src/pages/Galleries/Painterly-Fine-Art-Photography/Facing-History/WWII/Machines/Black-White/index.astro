---
export const prerender = true;
import GalleryShell from "@/components/GalleryShell-WWIIMachines-BW.astro";
import { entranceData as BWEntranceData } from "@/data/Galleries/Painterly-Fine-Art-Photography/Facing-History/WWII/Machines/BWEntranceData.ts";

const match = Astro.url.pathname.match(/\/(i-[a-zA-Z0-9]+)/);
const initialImageId = match ? match[1] : undefined;

console.log("🔥 initialImageId:", initialImageId); // ✅ Add this
---

<GalleryShell
  breadcrumb={BWEntranceData.breadcrumb}
  entranceData={BWEntranceData}
  initialImageId={initialImageId}
/>
