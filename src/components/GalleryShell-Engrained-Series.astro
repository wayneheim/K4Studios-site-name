---
import GalleryShellCore from "@/components/GalleryShellCore.astro";
import ChapterViewer from "@/components/Chapter-Engrained-Series.jsx";
import { galleryData as allImages } from "@/data/Other/K4-Select-Series/Engrained/Engrained-Series.mjs";
import { landingWestern as landingData } from "@/data/Other/K4-Select-Series/Engrained/landingstones.ts";

const GALLERY_PATH = "/Other/K4-Select-Series/Engrained/Engrained-Series";
const FALLBACK_DESCRIPTION = "Discover Engrained Series: unique fine art photographs printed on wood using Wayne Heim's multi-layer UV technique.";

const { breadcrumb, entranceData, initialImageId } = Astro.props;
const shouldSsrChapterViewer = Boolean(initialImageId && initialImageId !== "i-k4studios") || Astro.url.searchParams.get("view") === "grid";
---

<GalleryShellCore
  allImages={allImages}
  landingData={landingData}
  galleryPath={GALLERY_PATH}
  baseFallbackDescription={FALLBACK_DESCRIPTION}
  breadcrumb={breadcrumb}
  entranceData={entranceData}
  initialImageId={initialImageId}
>
  {shouldSsrChapterViewer ? (
    <ChapterViewer slot="chapter-viewer" client:load initialImageId={initialImageId} />
  ) : (
    <ChapterViewer slot="chapter-viewer" client:only="react" initialImageId={initialImageId} />
  )}
</GalleryShellCore>
