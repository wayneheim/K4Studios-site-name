---
import GalleryShellCore from '@/components/GalleryShellCore.astro';
import ChapterGalleryDirect from '@/components/ChapterGalleryDirect.jsx';
import ChapterViewer from '@/components/Chapter-WesternNarrativesBW.jsx';
import { galleryData as allImages } from '@/data/Galleries/Painterly-Fine-Art-Photography/Facing-History/Wild-West/Western-Narratives/Black-White.mjs';
import { landingWestern as landingData } from '@/data/Galleries/Painterly-Fine-Art-Photography/Facing-History/Wild-West/Western-Narratives/landingstones.ts';
import { buildDirectImagePayload } from '@/utils/buildDirectImagePayload.js';

const GALLERY_PATH = '/Galleries/Painterly-Fine-Art-Photography/Facing-History/Wild-West/Western-Narratives/Black-White';
const FALLBACK_DESCRIPTION = 'Explore painterly Western narrative photography in black and white by Wayne Heim.';

const { breadcrumb, entranceData, initialImageId } = Astro.props;
const isDirectImagePage = Boolean(initialImageId && initialImageId !== 'i-k4studios');
const directImagePayload = isDirectImagePage
  ? buildDirectImagePayload({ allImages, initialImageId, galleryPath: GALLERY_PATH })
  : null;
---

<GalleryShellCore
  allImages={allImages}
  landingData={landingData}
  galleryPath={GALLERY_PATH}
  baseFallbackDescription={FALLBACK_DESCRIPTION}
  breadcrumb={breadcrumb}
  entranceData={entranceData}
  initialImageId={initialImageId}
>
  {directImagePayload ? (
    <ChapterGalleryDirect
      slot="chapter-viewer"
      client:load
      initialImageId={initialImageId}
      directImagePayload={directImagePayload}
      galleryPath={GALLERY_PATH}
    />
  ) : (
    <ChapterViewer slot="chapter-viewer" client:load initialImageId={initialImageId} />
  )}
</GalleryShellCore>
